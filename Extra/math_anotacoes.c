//#include <math.h>

// pow(2, 3) == 8; pow elava um numero
// sqrt(9) == 3; sqrt caulcula raiz quadrada
// ceil(float) == arredonda um float pra o inteiro de cima

// operador ternario ?
// max = (A>B) ? A : B; Z é igual a A se a condicional for veradeira ou a B se a condicional for falsa